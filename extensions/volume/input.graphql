query Input {
  cart {
    lines {
      quantity
      merchandise {
        ...on ProductVariant {
          id
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "discounts-plus", key: "volume-config") {
      value
    }
  }
}