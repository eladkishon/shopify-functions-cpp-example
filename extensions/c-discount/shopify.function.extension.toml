name = "c-discount"
type = "product_discounts"
api_version = "2022-07"

[build]
command = "em++ -std=c++1z src/main.cpp -o build/main.wasm"
path = 'build/main.wasm'

[ui.paths]
create = "/volume/new"
details = "/volume/:id" # Note that this isn't implemented in this tutorial

